# Sea-Saw CRM Unified Gateway Configuration
# This nginx gateway serves as the unified entry point for both frontend and backend

# Upstream definitions
upstream backend_server {
    # Backend service container name (from sea-saw-server repo)
    server sea-saw-backend:8000;

    # Optional: Add multiple backend instances for load balancing
    # server backend-2:8000;
    # server backend-3:8000;
}

upstream frontend_server {
    # Frontend service container name (from sea-saw-app repo)
    server sea-saw-frontend:80;
}

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=static_limit:10m rate=100r/s;

server {
    listen 80;
    server_name _;  # Replace with your domain in production

    # Increase client body size for file uploads
    client_max_body_size 100M;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss
               application/rss+xml application/atom+xml image/svg+xml
               text/x-component text/x-cross-domain-policy;
    gzip_disable "msie6";

    # =============================================
    # Backend API Routes (Django)
    # =============================================

    # API endpoints with rate limiting
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://backend_server;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_redirect off;

        # Timeouts for long-running requests
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Django admin interface
    location /admin/ {
        limit_req zone=general_limit burst=50 nodelay;

        proxy_pass http://backend_server;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_redirect off;
    }

    # Django static files (CSS, JS, admin assets)
    location /static/ {
        limit_req zone=static_limit burst=200 nodelay;
        alias /home/app/web/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Django media files (user uploads)
    location /media/ {
        limit_req zone=static_limit burst=200 nodelay;
        alias /home/app/web/mediafiles/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Health check endpoint (no rate limit, returns 200 directly)
    location = /health/ {
        access_log off;
        return 200 '{"status": "ok"}';
        add_header Content-Type application/json;
    }

    # =============================================
    # Frontend Routes (React Native Web / Expo)
    # =============================================

    # All frontend requests are proxied to the frontend service
    location / {
        limit_req zone=general_limit burst=50 nodelay;

        proxy_pass http://frontend_server;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_redirect off;

        # Preserve original caching headers from frontend
        proxy_pass_header Cache-Control;
        proxy_pass_header Expires;
    }

    # Custom error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    # Logging
    access_log /var/log/nginx/gateway.access.log;
    error_log /var/log/nginx/gateway.error.log warn;
}
